import { ATTRIBUTION_TEXT } from 'zomboid-models';

export function App() {
  return (
    <main>
      <h1>zomboid-models playground</h1>
      <p>
        The viewer is not wired up yet. Point the playground at an asset folder built by the
        pipeline once both exist.
      </p>
      <footer>
        <small>{ATTRIBUTION_TEXT}</small>
      </footer>
    </main>
  );
}
